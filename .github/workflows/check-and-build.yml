name: Check and Build Android Dropbear
on:
  push:
    paths-ignore:
      - 'bin/**'
  workflow_dispatch:

env:
  SRC_OWNER: mkj
  SRC_REPO: dropbear
  FIXED_TAG: DROPBEAR_2020.81

permissions:
  contents: write
  pull-requests: write

jobs:
  check-and-build:
    name: Build Android Binary
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Download and Extract Dropbear Source
        run: |
          TAG="${{ env.FIXED_TAG }}"
          echo "Building Dropbear Tag: $TAG"
          curl -f -L "https://github.com/${{ env.SRC_OWNER }}/${{ env.SRC_REPO }}/archive/refs/tags/${TAG}.tar.gz" -o dropbear.tar.gz
          tar -xzf dropbear.tar.gz
          mv "dropbear-${TAG}" dropbear-src

      - uses: nttld/setup-ndk@v1
        with:
          ndk-version: r26b

      - name: Install Build Dependencies
        run: sudo apt-get update && sudo apt-get install -y autoconf automake libtool

      - name: Compile for Android ARM64
        run: |
          cd dropbear-src
          export TOOLCHAIN=$ANDROID_NDK_ROOT/toolchains/llvm/prebuilt/linux-x86_64
          export TARGET=aarch64-linux-android
          export API=21
          export CC=$TOOLCHAIN/bin/${TARGET}${API}-clang
          export AR=$TOOLCHAIN/bin/llvm-ar
          export STRIP=$TOOLCHAIN/bin/llvm-strip
          
          export CFLAGS="-O2"
          export LDFLAGS="-static"

          autoreconf -fi
          
          # OLD VERSION FIX: 2020.81 માં password auth easy disable થાય છે
          ./configure --host=$TARGET \
            --disable-zlib \
            --disable-utmp \
            --disable-wtmp \
            --disable-lastlog \
            --disable-password-auth \
            --disable-pam \
            --enable-static \
            ac_cv_func_getpgrp_void=yes

          make PROGRAMS="dropbear dbclient dropbearkey" STATIC=1 MULTI=1 -j$(nproc)
          $STRIP dropbearmulti
          mv dropbearmulti ../sshd_64
          chmod +x ../sshd_64
          file ../sshd_64

      - uses: actions/upload-artifact@v4
        with:
          name: sshd_64-android-arm64
          path: sshd_64

      - name: Commit Binary
        run: |
          mkdir -p bin
          mv sshd_64 bin/
          git config user.name "github-actions"
          git config user.email "actions@github.com"
          git add bin/
          git commit -m "Update sshd_64 ${{ env.FIXED_TAG }}" || true
          git push
